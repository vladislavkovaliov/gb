#!/bin/bash
# Smart git branch helper — устойчив к "fatal: not a valid object name"

set -e  # останавливает выполнение при любой ошибке

if [ -z "$1" ]; then
  echo "❌ Укажи название ветки, например:"
  echo "   gb N2PPOD-2222-feature"
  echo "   gb dev"
  exit 1
fi

branch_name="$1"

# Ветки без выбора типа
no_type_branches=("dev" "develop" "main" "master" "staging" "release")

# Проверка наличия gum
if ! command -v gum >/dev/null 2>&1; then
  echo "⚠️  gum не установлен. Установи его через:"
  echo "   brew install charmbracelet/tap/gum   # macOS"
  echo "   sudo apt install gum                 # Ubuntu/Debian"
  exit 1
fi

# --- Функция: безопасный переход / создание ветки ---
checkout_or_create_branch() {
  local new_branch="$1"

  git fetch --qui[48;47;171;1598;2736tet

  # Если ветка существует локально
  if git rev-parse --verify "$new_branch" >/dev/null 2>&1; then
    echo "🔁 Переключаюсь на локальную ветку '$new_branch'..."
    git checkout "$new_branch"
    return
  fi

  # Если есть в origin
  if git ls-remote --exit-code --heads origin "$new_branch" >/dev/null 2>&1; then
    echo "🌐 Найдена ветка '$new_branch' в origin. Создаю локальную копию..."
    git fetch origin "$new_branch":"$new_branch"
    git checkout "$new_branch"
    return
  fi

  # Определяем безопасную базу
  base_branch=""
  if git show-ref --verify --quiet refs/remotes/origin/develop; then
    base_branch="origin/develop"
  elif git show-ref --verify --quiet refs/remotes/origin/main; then
    base_branch="origin/main"
  elif git show-ref --verify --quiet refs/heads/develop; then
    base_branch="develop"
  elif git show-ref --verify --quiet refs/heads/main; then
    base_branch="main"
  else
    echo "⚠️  Не найдены origin/main или origin/develop — создаю ветку от текущей HEAD."
    base_branch="HEAD"
  fi

  echo "✨ Создаю новую ветку '$new_branch' от $base_branch..."
  git checkout -B "$new_branch" "$base_branch"
}

# --- Проверяем: нужна ли интерактивная часть ---
if [[ " ${no_type_branches[*]} " =~ " ${branch_name} " ]]; then
  checkout_or_create_branch "$branch_name"
  exit 0
fi

# --- Остальные ветки: показываем выбор типа ---
types=("fix" "feat" "refactor" "ci" "chore" "test" "docs")
selected_type=$(gum choose "${types[@]}")

if [ -z "$selected_type" ]; then
  echo "❌ Выбор отменён."
  exit 1
fi

new_branch="${selected_type}/${branch_name}"
checkout_or_create_branch "$new_branch"
